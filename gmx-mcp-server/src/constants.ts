// GMX-Zugangsdaten für IMAP (Lesen) und SMTP (Senden). Feste Werte, da GMX für alle
// Kund:innen dieselben Server nutzt — nur Adresse/Passwort sind pro Nutzer:in verschieden.
export const GMX_IMAP_HOST = "imap.gmx.net";
export const GMX_IMAP_PORT = 993;

export const GMX_SMTP_HOST = "mail.gmx.net";
export const GMX_SMTP_PORT = 587;

export const CHARACTER_LIMIT = 25000;
export const DEFAULT_SEARCH_LIMIT = 20;
export const MAX_SEARCH_LIMIT = 100;
export const BODY_PREVIEW_LIMIT = 3000;
